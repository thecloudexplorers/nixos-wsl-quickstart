{
    ...
}:

{
    programs = {
        git = {
            enable = true;

            lfs = {
                enable = true;
            };

            prompt = {
                enable = false;
            };
        };
    };

}